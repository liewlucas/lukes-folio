---
// Fixed, non-interactive texture overlays: woven linen + paper grain.
// Sit below content (which uses .above-texture). See src/styles/texture.css.
---

<div class="texture-linen" aria-hidden="true"></div>
<div class="texture-grain" aria-hidden="true"></div>
