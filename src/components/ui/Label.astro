---
interface Props {
  red?: boolean;
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}
const { red = false, as = "span", class: cls = "" } = Astro.props;
const Tag = as;
---

<Tag class:list={["label", red && "label-red", cls]}>
  <slot />
</Tag>
